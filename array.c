#include<stdio.h>
int main()
{
    int a[4],i,sum=0;
    float avg;
    printf("Enter 4  numbers");

    for(i=0; i<=3; i++)
        scanf("%d",&a[i]);

    for(i=0; i<=3; i++);
    sum =sum+a[i];
    avg =sum/4.0;
    printf("avg is %f",avg);

}

